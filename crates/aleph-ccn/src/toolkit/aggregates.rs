//! Aggregate helpers.
//!
//! Port of `src/aleph/toolkit/aggregates.py`.
//!
//! The Python source file is currently empty, so this module is intentionally
//! a placeholder with no public items. It is kept so that future shared
//! aggregate utility helpers can land alongside the matching Python ones
//! without renaming.

#[cfg(test)]
mod tests {
    #[test]
    fn module_compiles() {
        // Sanity: ensure the empty module compiles.
    }
}
